# Install Puppeteer-core in Nodejs project
$ npm install puppeteer-core

# Install Libraries for Puppeteer
sudo apt-get install -y \
  libnss3 \
  libatk1.0-0 \
  libatk-bridge2.0-0 \
  libcups2 \
  libdrm2 \
  libxkbcommon0 \
  libxcomposite1 \
  libxdamage1 \
  libxfixes3 \
  libxrandr2 \
  libgbm1 \
  libasound2 \
  libcairo2 \
  libpango-1.0-0

# Download Chrome Headless version and unzip it
https://googlechromelabs.github.io/chrome-for-testing/

# You can use it in your Node file as exceutable
const browser = await puppeteer.launch({ 
  headless: true,
  executablePath: '/usr/bin/chromium-browser'
});